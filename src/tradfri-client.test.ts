import { expect } from "chai";
import "./"; // dummy-import so index.ts is covered
// tslint:disable:no-unused-expression

describe("tradfri-client => method() =>", () => {

	it("should work", () => {
		// ok
	});

});
